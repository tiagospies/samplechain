/** Define o padrão para contratos de Moeda. */
interface ICoinContract {
    
    /** Valida se a carteira para a qual a quantidade que está sendo transferida é válida. */
    isValidWallet: boolean;

    accountHave
}